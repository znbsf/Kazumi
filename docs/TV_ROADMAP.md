# Kazumi TV 开源路线图

本文件是 Kazumi TV 分支的阶段计划与历史验收台账；当前状态及 UI 待办编号统一见
[README](../README.md#ui-plan)，历史轮次的完成项不代表新发现的问题已修复。项目继续遵守上游
GPL-3.0；TV 公开测试 APK 使用自有几何电视标识，不打包需单独授权的上游人物图标。

## 当前公开测试版 Preview 3

- `v2.3.0-tv-preview.3` / `203026` 发布同一真机验证 APK，仅重命名文件，内部版本仍为 `2.3.0-tv-device-test`。
- UI-01～UI-04 焦点、搜索和历史修复，追加旧滚动请求取消、两行选集标题行高，以及普通电视桌面 LAUNCHER 入口。
- 发布前 250 项测试通过；AVD 开发包完成组件 / 本地媒体回归，203026 完成小米电视桌面启动及短程真实续播检查。
- 本包不含演示弹幕或在线弹幕凭证；暂停 seek 进度保存（UI-13）、网络错误反馈（UI-10）、上游渠道文案（UI-12）仍未修复。
- [下载 Preview 3](https://github.com/znbsf/Kazumi/releases/tag/v2.3.0-tv-preview.3) · [发行说明](TV_PREVIEW_3.md) · [真机记录](TV_DEVICE_203026.md)。

## 公开测试版 Preview 2（历史）

- 版本 `v2.3.0-tv-preview.2` / `203010`，发布已在 AVD 与实体电视核对过哈希的同一 APK。
- 包含详情页主操作区、菜单逐层返回、透明选集、播放器循环、主页入口和详情媒体键续播。
- 发布前 216 项测试与修改的 Dart 文件静态分析通过；不把测试数量当作全部设备验收。
- 此公开 APK 的首页首列回侧栏与选集双描边问题仍存在；开发分支进展见下一节。验证码和实体遥控器长返回保留待办。
- 下载：[TV Preview 2](https://github.com/znbsf/Kazumi/releases/tag/v2.3.0-tv-preview.2)；[完整说明](TV_PREVIEW_2.md)。

下方保留各轮历史测试记录；当时的“不发布/不操作真机”描述仅适用于该轮，后续发布及真机证据单独记录。

## 发布后 UI 审查与本地修复（2026-09-06）

以下保留本轮当时的状态：当时公开 APK 为 Preview 2 / 203010，修复在独立分支 `codex/tv-focus-ui01-ui04`，
不推进发行标签或更新线上附件；该轮只操作独立 Google TV 模拟器，不操作实体电视。
后续合并、发布及用户授权的真机测试见上方 Preview 3，不将各轮证据混为一次验收。
完整 UI-01～UI-12 待办及验收要求记录在 [README 的 UI 计划](../README.md#ui-plan)。

- **UI-01 / UI-02 本地实现：**显式侧栏出口与焦点恢复；当前焦点独占粗框，播放 / 分类选中保留独立状态标识。
  真实壳层及深浅选集组件有自动测试，AVD 使用离线样例；实际视频面板及实体电视仍待复测。
- **UI-03 / UI-04 本地实现：**先在旧版实际组件复现搜索 OK 与历史编辑 OK 风险，再验证新输入入口、原生返回、
  历史更多操作 / 删除确认 / 恢复焦点。测试历史与续播目标均为模拟，不删除用户数据，不宣称网络续播成功。
  详细命令、APK 身份和证据边界见[本轮回归记录](TV_FOCUS_REGRESSION.md#ui01-ui04-local)。
- **UI-04 审查补修：**原 235 项测试漏掉滚动中快速转向。追加卡片本地输入取消通知、原焦点归属检查与旧滚动取消，
  正式回归覆盖 10 ms 转向、更多菜单 / 原生返回、连续 DOWN，并检查新焦点仍可见。
  见[时序补修记录](TV_FOCUS_REGRESSION.md#ui04-race-followup)，仍未合并或发布。
- **下一批 UI-05～UI-10：**侧栏名称、续播提示、播放器常用 / 更多操作、选集下载入口、
  选源双栏和明确的加载 / 错误反馈。保留现有风格、播放核心及有效用户设置。
- **随后 UI-11 / UI-12：**分类溢出和 LCN 范围提示，以及设置、时间表、追番、下载等次级页面回归。
  设置已有横向双栏与 TV 默认配置，不重复建设。

每项按“进入 → 方向移动 → 确认 → 子菜单 → 返回 → 恢复原焦点”验收，区域循环必须保留出口；
当前焦点与在播 / 已选中状态需明显区分。先使用模拟器及模拟历史验证，不删除用户真实历史；
真机安装和操作另按当轮用户授权执行。未验证前不把待办写入 Release 的“已修复”。

第二阶段的手机接力（节目、来源、集数、时间点）及电视台台标角标仍是后续计划，
上游已有 DLNA / 同步相关代码不代表这些目标已经完成。

## 公开测试版 Preview 1（历史）

- [x] OK 唤醒固定焦点、选集入口、输入框显式唤起键盘。
- [x] 左栏集成历史记录，并保留手机版导航结构。
- [x] 审查电视默认配置、压缩数据缓存预算和设置保留机制，见 [审查表](TV_DEFAULTS.md)。
- [x] 本地合成弹幕仅由预览构建脚本显式注入，逐条标注来源和未配置凭证的原因。
- [x] Release 模式同一 APK 在 TV AVD 与 MiTV-ASTP0 复测，并核对安装后的 SHA-256。

下载入口：[TV Preview 1](https://github.com/znbsf/Kazumi/releases/tag/v2.3.0-tv-preview.1)。

公开测试边界及复现方式见 [Preview 1 说明](TV_PREVIEW_1.md)。

## 透明面板与遥控导航（203007 本地测试历史）

- [x] TV 选集面板移除内层不透明背景，只保留 64% 不透明度的主题底板；保留原有主题色和焦点描边。
- [x] 选集入口移到播放键旁，TV 底栏集中排列；播放器两排按钮左右首尾、上下跨排循环。
- [x] 顶部分类、操作设置页签与侧栏循环；主页保留首列左键回侧栏、侧栏左键到右侧的通路。
- [x] 长按返回约 1 秒后松开直达 LCN 主页；短按逐层返回，系统 Home 不拦截；播放器提供可见主页入口。
- [x] 详情页播放键优先通过现有历史播放服务续播；没有可用历史则打开选源，禁止猜测播放源。
- [x] 相关自动测试 22 项、完整测试集 208 项通过，修改文件静态检查通过。
- [x] Google TV AVD 验证短/长返回、播放器循环、选集透明、详情页续播与无历史选源；最终本地测试包 versionCode 203007。
- 本轮不安装实体电视、不更新公开 Release；验收详情继续记录在 [焦点回归记录](TV_FOCUS_REGRESSION.md)。

## 详情页主操作区（本地回归包 203010）

- [x] TV 移除右下角播放/吐槽悬浮按钮，将开始观看、追番、发表吐槽放在海报旁、标题下的一排；沿用原主题色、圆角与评分内容。
- [x] 进入详情默认聚焦开始观看；主操作左右首尾循环，上键到返回，下键到当前页签；页签上键展开头部并回到播放，已可见页签不强制卷走头部。
- [x] 追番菜单打开时聚焦当前状态；短按返回只关闭菜单，恢复入口焦点，再次返回才离开详情。
- [x] 8 项新增测试（含真实追番组件与 native maybePop 路径、手机版卡片、长标题及大字体），完整测试集 216 项通过。
- [x] TV Release 203010 安装 Google TV API 36 AVD 并核对 APK 哈希；实际方向键、默认焦点、无历史 OK 选源，以及尼古喵喵详情 OK 直接续播并出画验证。
- 不更改手机版布局或操作手机设备；未安装实体电视、未发布新 Release。媒体源实际出画与界面入口验收分开记录，见 [焦点回归记录](TV_FOCUS_REGRESSION.md)。

## 随后追加：203010 实体电视测试（2026-09-05）

用户明确要求实际测试后，保留数据安装到 MiTV-ASTP0，安装后 APK 哈希核对一致。

- [x] 实体电视详情布局、默认焦点、主操作循环、追番菜单短返回。
- [x] 明确选源后真实视频出画；回详情用媒体 Play 恢复同集进度并再次出画。
- [x] 浅色选集透明、选集行循环及底部回首行、OK 唤醒、发送弹幕聚焦不弹键盘、可见主页按钮返回 LCN。
- [ ] 真机复现首页首列左键转到顶部分组，未稳定回侧栏；需修复并补完整页面焦点测试。
- [ ] 区分正在播放集数的状态标识与遥控器当前焦点，避免两个相同粗绿色框。
- [ ] 实体遥控器 900 ms 长返回、完整厂商键码、长时间音画同步仍未验收。

本轮只测试并记录，不修改应用代码、不发布新 Release；详见 [真机回归记录](TV_FOCUS_REGRESSION.md)。

## 新增待办：按来源诊断验证码

已接收 mutefun 图片加载、dalvdm 提交定位、mgnacg 成功检测三个不同故障的交接；
未把它们合并归因于旧 WebView 或无头渲染。诊断与安全日志改造尚未完成，见
[验证码诊断计划](TV_CAPTCHA_DIAGNOSTIC_PLAN.md)。

## 产品目标

在同一个开源仓库内保留 Kazumi 的配色、排版和内容结构，同时让手机与
Android TV/Google TV 各自拥有适合其输入方式的界面。共享规则解析、历史记录、
播放器和数据模型，不复制出两套不可同步的业务代码；Android 端通过 product
flavors 分别发布手机与电视安装包。

## 阶段总览

| 阶段 | 目标 | 当前状态 |
| --- | --- | --- |
| 第一阶段 | 可独立安装和使用的 Android TV 版本 | Preview 3 已公开测试；已有 AVD 与 Android 9 真机记录，进度保存、导航、来源兼容及长期稳定性仍待收口，不是稳定版完成 |
| 第二阶段 | 手机发现电视并从当前节目、来源和时间点接力播放 | 待第一阶段验收后开始 |

## 第一阶段：Android TV 版本

### 构建结构

| Flavor | 安装包 | Application ID | 启动入口 |
| --- | --- | --- | --- |
| `mobile` | Kazumi Mobile | `com.predidit.kazumi` | `LAUNCHER` |
| `tv` | Kazumi TV | `com.znbsf.kazumi.tv` | `LEANBACK_LAUNCHER` + `LAUNCHER`（同一 TV Activity） |

两个 flavor 共享 `lib/`、播放器、规则和数据层，但 Manifest、包名、应用名称和
TV Banner 分开。它们可以同时安装，也可以独立发布和更新。

本机构建命令：

```powershell
flutter build apk --debug --flavor mobile
flutter build apk --debug --flavor tv
```

Android Studio 的 Build Variants 中选择 `mobileDebug` 或 `tvDebug`；运行目标分别
使用手机 AVD 或 Google TV AVD。

如果 Android 模拟器需要复用宿主机 HTTP 代理，可只为 Debug 构建显式传入代理地址：

```powershell
flutter build apk --debug --flavor tv --dart-define=KAZUMI_DEV_PROXY=10.0.2.2:10646
```

其中 `10.0.2.2` 是 Android Emulator 访问宿主机的地址。该参数在 Release 构建中
不会生效；未传入时也不会改变用户已有的代理设置。

### 范围

- 同一仓库分别产出手机 APK 与 TV APK，不再把两类启动入口合并到一个安装包。
- 原生识别电视设备；只在电视上锁定横屏并启用电视交互模式，不改变手机版行为。
- 保持 Kazumi Material 3 视觉语言，为十英尺观看距离增加明确的焦点描边、适度放大和自动滚动可见性。
- 首页、时间表、追番、历史等主要内容卡片可用 D-pad 聚焦和确认键打开。
- TV 首页按从左到右、从上到下的顺序显示节目号；数字键支持 1–3 位输入、
  1.8 秒等待、匹配高亮、自动滚动和无对应编号的末尾提示。单次查找
  最多额外加载 5 页或等待 10 秒，返回/切页可取消；超限不误报为不存在。
- TV 顶部横向平铺番组分类；左右聚焦时放大突出，停留 280ms 后联动更新下方卡片；
  快速跨过中间项不触发无用网络请求。手机版继续使用原有折叠下拉菜单。
- 侧边导航和搜索入口可以通过 D-pad 连续访问，返回键行为可预测。
- TV 侧栏“遥控器”作为操作设置的快捷入口；同一页面横向切换固定遥控器说明与可编辑的通用按键，播放器内仍保留快捷帮助面板。
- 播放器支持电视遥控器的确认键、方向键和媒体键；控制层显示时，方向键用于控件导航。
- TV 播放器初始选集面板采用较窄的半透明四列布局；当前集与焦点项都有独立描边。
- 播放控制层使用显式焦点拓扑：左右按控件顺序，Up/Down 在顶栏和底栏间映射；
  播放进度条和仅用于快捷键接收的父焦点不进入可见遍历链。
- TV 播放页映射 EPG/Guide、字幕/CC（切换弹幕）、收藏、信息、频道、音量、返回、停止和退出键；
  红/绿/黄/蓝功能键分别作为弹幕、收藏、选集和详情的兼容入口。
- INFO/蓝键显示 mpv 实际解码通路、视频输出、帧率、缓存和丢帧；HELP/MENU 显示遥控器键表；
  设计来源与取舍记录在 `docs/TV_OPEN_SOURCE_REFERENCES.md`。
- 保留规则、弹幕、字幕、超分辨率和播放源能力，不通过删功能换取 TV 兼容。
- TV“自动”视频输出使用 `mediacodec_embed` 直连 Surface；用户显式渲染器设置和
  手机版自动选择保持不变，直连输出会阻止不兼容的超分辨率。
- 弹幕凭证通过 `dart-define` 注入，不写入仓库；缺失时请求前失败。弹幕调度支持
  同秒去重、短跨度追帧以及 seek/暂停/重载后的旧任务失效。

### 第一阶段验收门槛

- [x] `tv` Manifest 声明 Leanback 入口且不要求触摸屏，`mobile` Manifest 只声明普通启动入口。
- [x] 真机/模拟器能识别 TV 模式；手机模式回归不被锁定横屏。
- [ ] 启动后无需鼠标即可进入推荐页、搜索、详情页、选择来源并开始播放。
- [x] 所有主要聚焦项都有清晰焦点状态，长列表移动焦点时目标保持可见。
- [x] 首页数字键节目号能处理单位、两位和三位输入；未加载到的编号按页数/时间上限停止，且可用返回键取消。
- [x] TV 分类栏横向显示、焦点状态清晰，切换后下方卡片跟随更新；手机交互不变。
- [x] 遥控器说明与操作设置已合并，可从 TV 侧栏直接进入、横向切换并用 D-pad 滚动。
- [x] 播放器遥控器键位和事件分发已实现并通过 widget/unit tests；真实视频源操作并入完整路径冒烟测试。
- [x] `flutter analyze` 无新增 warning，相关 widget/unit tests 通过。
- [x] Android TV APK 构建成功并安装到 TV AVD。
- [x] 在至少一个 1080p Android TV/Google TV AVD 完成完整路径冒烟测试。
- [x] AVD 验证半透明四列选集面板可用，当前集与焦点项可区分，左右/上下移动及首列退出连续。
- [x] AVD 验证播放器隐藏控制层后 Up/Down 可进入对应行；顶栏 Right、跨行 Down、
  底栏 Left/Right 均有可见描边且不会触发方向键 seek。
- [x] 弹幕时间轴和凭证边界有单元测试；无凭证构建不会发出空签名请求。
- [x] 实体小米电视完成对应 ABI 的 TV APK 安装、Leanback 启动和横屏首次引导验证。
- [ ] 实体电视完整遥控器键码、真实视频硬解和长时间播放仍待人工验收；安装成功不能替代这些项。

> 2026-09-05 用户将本轮目标明确收束为虚拟机修好并验收；因此本轮不连接、不安装、
> 不调试实体电视。上面保留此前已经发生的真机安装记录作为历史证据，不把它当作本轮门槛。

### 2026-09-04 验证记录

已观察并确认：

- 基于上游提交 `5d1569b`，开发分支为 `codex/android-tv-phase1`。
- API 36 Google TV x86_64 AVD `Kazumi_TV_API_36` 以 1920×1080 横屏识别为
  `television`；系统能通过 `LEANBACK_LAUNCHER` 查询并启动主 Activity。
- 清除应用数据后，只使用遥控器确认键完成四步引导并进入主界面；主界面焦点会恢复到
  搜索入口，确认键进入搜索页。方向键、确认键和 Back 也完成了侧栏→历史记录→返回的
  实机事件冒烟测试。
- API 35 手机 AVD `Medium_Phone_API_35` 以 1080×2400 竖屏启动最初的通用 APK，
  保留原有底部导航和手机版布局。
- TV 卡片确认键、焦点包装器、播放器确认键和媒体键事件分发有自动化测试覆盖；完整
  上游测试套件、静态分析和 Debug APK 构建通过。
- flavor 拆分后，`app-mobile-debug.apk` 与 `app-tv-debug.apk` 分别构建成功；ABI
  拆分的 mobile/TV release APK 也分别完成真实构建。
- APK 最终 Manifest 已验证：mobile 包名为 `com.predidit.kazumi`，只有普通
  `LAUNCHER`；TV 包名为 `com.znbsf.kazumi.tv`，只有 `LEANBACK_LAUNCHER`，并且
  Leanback 为必需、触摸屏为非必需。
- 独立 TV 包在 `Kazumi_TV_API_36` 冷启动并仅用确认键完成引导进入搜索；独立 mobile
  包在 `Medium_Phone_API_35` 冷启动并保持 1080×2400 竖屏。两个包没有复用彼此的
  Application ID 或启动入口。
- TV Debug 构建通过显式的 `KAZUMI_DEV_PROXY=10.0.2.2:10646` 连接宿主机 HTTP
  代理；规则目录请求返回 HTTP 200，首次引导的“添加规则”页实际显示 `moonci`、
  `mutefun` 等在线来源和安装按钮。
- 通过 `7sefun` 完成详情→选择播放源→选集→真实视频播放；退出全屏后窗口保持
  1920×1080 横屏，不再被 540dp 短边误判为手机并锁成竖屏。
- AVD 实际注入并确认 INFO 打开视频详情、黄色功能键打开选集侧栏；Android 原生
  桥接也覆盖 EPG/Guide、字幕/CC（映射为弹幕开关）、收藏与四色功能键。标准 EPG 键在 AVD 中被系统层
  截获，仍保留到实体遥控器验收。
- 历史记录卡片高度与内部海报、边距和内边距一致，不再出现底部 2px overflow。
- INFO 状态页在 TV AVD 的真实网络视频上读到 `hwdec-current=mediacodec-copy`、
  `current-vo=gpu-next` 和 `current-gpu-context=android`；这证明 MediaCodec 通路已接入，
  但模拟器结果不等同于实体电视芯片硬解证明。
- TV Release 构建在同一 AVD 上显示 1–24 号首页角标；实际输入 `2` 进入第 2 项，
  连续输入 `21` 会滚动并进入第 21 项，输入 `99` 会自动加载并进入第 99 项；
  输入 `999` 会在 5 个额外分页后停止，输入 `888` 后按返回键会立即取消且仍停留首页。
- TV 顶部分类栏已在 AVD 上用方向键从“热门番组”切换到“日常”和“原创”，
  焦点描边/放大与下方列表刷新均可见；侧栏“遥控器”快捷入口及说明/自定义按键横向切换完成可见路径验证。
- Android 本地产物命名为 `Kazumi-TV-<version>-<buildType>.apk` 和
  `Kazumi-Mobile-<version>-<buildType>.apk`；GitHub 发行文件名额外标记 TV/Mobile 和 ABI。
- `Kazumi-TV-2.3.0-armeabi-v7a-release.apk` 已安装到 `MiTV-ASTP0`，系统确认
  `com.znbsf.kazumi.tv` 版本 `2.3.0`（versionCode `203001`）并通过 Leanback 启动；
  首次免责声明页面以 1920×1080 横屏显示。尚未代用户同意条款或完成真机来源/播放验证。

### 2026-09-05 模拟器收尾记录

- 用户要求释放实体电视，本轮之后所有构建安装、播放路径和截图均只在
  `emulator-5554`（Google TV API 36，1920×1080）完成。
- 选集侧栏缩为约 42% 屏宽（限制在 360–440 logical px）、0.88 背景透明度、四列
  52 px 卡片；AVD 实测当前集高亮、Right、Down、首列 Left 关闭和自动保持可见。
- 修复控制层自动隐藏计时器在选集面板可见时抢回播放器焦点；TV 焦点包装器不再让
  内部 `InkWell` 形成不可见的二次焦点。
- 播放控制层加入 TV 专属焦点主题和显式焦点拓扑。AVD 连续验证：隐藏时 Up → 顶栏
  “跳过片头” → Right 到“画中画” → Down 到底栏“倍速” → Left 到“超分辨率”；
  每一步都有边框，进度只随实际时间推进。
- 进度条和全局快捷键接收节点从 TV 遍历链排除；控制层可见时方向键先作为导航，
  不再落入快进/快退快捷键。
- 无 `DANDANAPI_APPID` / `DANDANAPI_KEY` 的公开构建会在联网前返回明确的缺凭证状态；
  日志确认未再向弹弹 play API 发出空签名请求。时间轴测试覆盖慢速同秒去重、最多
  3 秒短跨度追帧、大幅/反向 seek 和 reset。
- 既有实体电视 A/B 记录显示：1080p 在 `mediacodec-copy + gpu` 下约
  `0.871x–0.915x`，切换直连 Surface 后约 `0.99990x–0.99998x`，输出丢帧 `0`、
  A/V 差约 `0.02 ms`；480p 旧路径约 `1.00075x`。据此 TV“自动”选择
  `mediacodec_embed`，显式渲染器与手机版逻辑不变。

仍待确认：

- 当前 AVD 的直连网络仍不可用，需要显式复用宿主机代理。
- 真实电视/盒子的标准 EPG 键码、4K/HDR、完整焦点观感和长时间播放仍留待以后；
  本轮按用户要求不再操作真机。
- 官方在线弹幕仍需要维护者自行申请的 AppId/AppSecret；公开仓库不会内置或提交密钥。

### 第一阶段非目标

- 不实现手机发现电视、配对或远程控制。
- 不实现跨公网播放接力。
- 不为 TV 单独维护规则格式或复制播放器实现。

## 第二阶段：手机与电视联动

### 用户路径

1. 手机与电视位于同一局域网，手机发现 Kazumi TV。
2. 首次通过电视配对码或二维码建立本地信任。
3. 手机播放页点击“在电视播放”。
4. 手机发送规则来源、节目、剧集、临时播放信息和当前位置。
5. 电视准备成功并 seek 到目标位置后返回确认，手机才暂停本地播放。
6. 电视持续回传播放状态；停止或切集时同步最新历史进度。

### 协议边界

播放接力消息使用可版本化的数据模型，至少包含：

- `protocolVersion`、`requestId`、配对设备标识和短期授权令牌；
- `ruleId/sourceId`、节目稳定 ID、剧集稳定 ID、标题和封面；
- 来源页面、视频/音频 URL、必要且最小化的请求头及失效时间；
- `positionMs`、时长、发送时间、弹幕与字幕标识；
- TV 的 `accepted`、`ready`、`playing` 或结构化失败响应。

电视优先按语义信息重新解析来源；手机传来的临时播放 URL 作为快速路径。不得在
日志中记录 Cookie、Authorization 或完整签名 URL。电视确认播放前手机不得停止，
避免出现两端都不播放的失败状态。

### 第二阶段验收门槛

- [ ] 同一局域网自动发现、显式配对、取消配对和令牌轮换。
- [ ] 从手机当前节目、当前来源、当前集和当前时间点接力到电视。
- [ ] DASH 分离音视频、HLS、需要 Referer/Cookie 的来源都有失败回退。
- [ ] 手机可控制电视播放/暂停、seek、切集和停止。
- [ ] 断线重连、重复请求幂等、URL 过期和电视忙状态均有明确行为。
- [ ] 电视退出播放时将最终进度同步回手机，冲突按更新时间和播放会话处理。
- [ ] 敏感请求头不落盘、不进日志，局域网未配对设备不能发起播放。

## 后续路线待办

- [ ] 手机与 TV 的局域网发现、配对、节目/来源/集数/时间点接力和双向播放控制；
  以上述第二阶段协议与验收门槛为准。
- [ ] 在番剧卡片角标展示日本实际播出电视台的台标。实现前必须先解决番组 ID 与
  播出表的稳定映射、日本时区与联播台问题、台标授权/归属标注、数据缓存与过期策略；
  无可验证数据时不展示猜测台标。

## 维护约定

- 上游远端使用 `upstream`，个人 fork 使用 `origin`。
- TV 改动优先做成可检测、可复用的适配层，避免长期偏离上游。
- 每个验收项只在有构建、测试或人工路径证据后勾选。
- 模拟器结果、真实设备结果和仅静态审查结果必须分开记录。
